[package]
name = "isel"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
ir = { path = "../ir" }
device = { path = "../device" }
banking = { path = "../banking" }
iselcore = { path = "../iselcore" }

[dev-dependencies]
pic14-sim = { path = "../sim" }
asm = { path = "../asm" }
legalize = { path = "../legalize" }

[[bin]]
name = "isel"
path = "src/bin/isel.rs"
