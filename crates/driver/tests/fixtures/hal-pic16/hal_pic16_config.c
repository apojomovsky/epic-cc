/* Vendored config-word TU for the PIC16F877A (CC-3 shape, the same role
 * hal-pic18_config.c plays for the 4550): a single EPIC_CONFIG
 * invocation the driver's pre-scan resolves. */

#include "epic-cc.h"
EPIC_CONFIG("bor=on, osc=hs, lvp=off, pwrt=on, wdt=on, wrt=off, xtal_hz=20000000");
