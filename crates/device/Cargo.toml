[package]
name = "device"
version = "0.1.0"
edition = "2021"
[build-dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
[dev-dependencies]
alloc = { path = "../alloc" }
asm = { path = "../asm" }
ir = { path = "../ir" }
